# thinety.github.io

Source code for my personal website. Run `npm install` to set up the environment.

```bash
# start local development server
npm run dev

# compile CV, typst should be locally installed
typst compile cv.typ public/cv.pdf
```
